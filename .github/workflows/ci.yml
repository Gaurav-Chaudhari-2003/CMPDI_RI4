name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ dev ]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI is active"
